xlsx 템플릿 시트파일을 위한 jinja2 같은 렌더링라이브러리를 만드려고 한다.

전체 기능이 필요한건 아니다.

주요 기능은, 
시트내의 서식은 유지해야한다. 
제어문, 변수, 주석을  지원해야 한다.
for, if 는 지원해야 한다.
렌더링이 끝난후에는 템플릿 문법을 모두 제거해야 한다.
복잡성을 없애기 위해 몇가지 제약이 있다.

 
시트의 첫번째 컬럼에만 제어문과 주석이 존재해야 한다.
제어문과 주석은 하나의 셀을 전부 차지해야 한다.

하나의 셀안에  일반 텍스트와 제어문이 섞여 있으면 안된다.
제어문은 그 아래에 있는 row들에 대해서 루프를 돌던지, 조건적으로 적용되어야 한다.

변수는 변수치환이 가능해야 하고, 표현식을 지워하면 좋겠지만, 복잡한 표현식은 지원하지 않는다.
엑셀의 셀 내에 여러 변수가 들어갈수 있으므로, 엑셀에서 표현가능한 중복된 표현식은 지원안해도 된다. 가령 문자열 연결.
아래 표현식중, 간단한거만 우선 구현하자. 비교연산은 필요없다.

```
{# 산술 연산 #}
{{ 10 + 5 }}           → 15
{{ price * 1.1 }}      → 가격의 10% 추가
{{ age - 1 }}          → 24

{# 문자열 연결 #}
{{ "안녕" + "하세요" }}  → 안녕하세요
{{ name + "님" }}       → 홍길동님

{# 비교 연산 (True/False 출력) #}
{{ age > 20 }}         → True
{{ name == "홍길동" }}  → True

{# 삼항 연산자 #}
{{ "성인" if age >= 20 else "미성년자" }}
```

필터, 필터체이닝은 필요하지 않다.